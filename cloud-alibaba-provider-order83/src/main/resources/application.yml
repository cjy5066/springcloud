server:
  port: 83


spring:
  application:
    name: nacos-order-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

# 服务提供者（可写可不写，在这里）
service-url:
  nacos-user-service: http://nacos-payment-provider